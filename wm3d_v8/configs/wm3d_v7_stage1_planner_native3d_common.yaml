_base_: wm3d_v7_1b_native_actionpolicy_joint_formal100k_3node24_v3.yaml

# V7 Stage1-P is an isolated continuation of the completed Stage0 lineage.
# The three SHA placeholders must be replaced with produced artifacts; the
# preflight deliberately refuses to train while any placeholder remains.
contract:
  schema: wm3d_v7_stage1_planner_native3d_v1
  purpose: grounded_native_3d_imagination_and_action_selection
  preflight_required: true
  preflight_script: scripts/preflight_wm3d_v7_stage1_planner.py
  automatic_phase_promotion: false
  serving_action_owner: frozen_stage0_direct_pose_plus_delta_composed_gripper
  proposal_action_owner: frozen_stage0_pose_flow_plus_deterministic_grip_beams
  planner_action_inputs_forbidden: true
  future_observation_inputs_forbidden: true
  repeated_root_context_forbidden: true
  native_3d_outputs_required: [token, depth, point, pose]

planner_data:
  index: /data/Minko/world_model/wm3d_v7/manifests/robocasa_stage1_planner_h32_v2/index.jsonl
  index_sha256: REPLACE_AFTER_V2_BRANCH_CACHE_SEAL
  action_stats: /data/Minko/world_model/wm3d_v7/manifests/robocasa365_stage0_full_v1/action_stats_train.npz
  action_stats_sha256: 3fc7906d149e444e985742bcc9a632ec6a8e874ec4f7fcf95ae0102dd7491bb0
  context_frames: 16
  context_source: current_pinned_robocasa_runtime_causal_replay
  future_frames: 32
  action_history_len: 4
  branches: 11
  all_branch_native_geometry: true
  same_root_current_runtime_exact: true
  pseudo_outcomes: false

planner_stage:
  source_checkpoint: /data/Minko/world_model/wm3d_v7_actionrepair1b_20260806/results/wm3d_v7_1b_native_actionpolicy_joint_formal100k_3node24_v3/ckpt/step_00100000.pt
  source_checkpoint_sha256: REPLACE_AFTER_STAGE0_STEP100000_HARD_STOP
  num_nodes: 3
  gpus_per_node: 8
  batch_size_per_gpu: 1
  gradient_accumulation_steps: 4
  effective_global_batch: 96
  candidate_microbatch: 1
  activation_checkpointing: true
  num_workers: 2
  seed: 27081
  sampler_seed: 27081017
  checkpoint_every_steps: 1000
  log_every: 10
  max_grad_norm: 1.0
  weight_decay: 0.01
  dynamics_weight: 1.0
  imagined_planner_weight: 0.5
  true_planner_weight: 1.0
  dynamics_loss:
    token_mse_weight: 1.0
    token_cosine_weight: 0.1
    effect_weight: 1.0
    depth_weight: 0.25
    point_weight: 0.25
    pose_weight: 0.1
    effect_floor: 1.0e-6
  planner_loss:
    progress_weight: 0.5
    success_weight: 1.0
    risk_weight: 0.5
    uncertainty_weight: 0.25
    ranking_weight: 1.0
    ranking_margin: 0.05
