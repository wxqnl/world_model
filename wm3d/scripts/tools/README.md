# Operator reports

`report_5b_run.py` summarizes data closure, training progress, throughput, gradient ownership,
checkpoint integrity, and offline evaluation. Use `--require-complete` in acceptance jobs; use
the default incomplete mode while a run is still progressing.
