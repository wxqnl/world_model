schema: wm3d_v8_model_profile_v1
name: native_1b_v8_core
architecture: native_world_model
expected_parameter_count: 1206327539
sampling:
  mode: observed_monotonic_subsequence
  history_action_leading_boundary: observed_previous_state
  context_horizon_seconds: 3.2
  future_horizon_seconds: 1.6
  minimum_horizon_coverage: 0.9
  minimum_anchor_separation_seconds: 0.2
  policy_target_horizon_seconds: 1.6
  policy_training_times: observed_action_timestamps
  interpolation: forbidden
model:
  schema: wm3d_native_world_model_v2
  T: 16
  P: 64
  K: 8
  token_dim: 2048
  task_dim: 2048
  num_views: 3
  state_hidden: 1600
  state_layers: 18
  state_heads: 16
  state_ff_mult: 2.5
  action_hidden: 1280
  action_layers: 14
  action_heads: 16
  action_ff_mult: 2.6666666666666665
  policy_task_modulation: true
  policy_calibration_conditioning: true
  bridge_layers_state: [2, 5, 8, 11, 14, 17]
  bridge_heads: 16
  dynamics_layers: 1
  factual_dynamics_repeats: 2
  factual_action_residual_scale: 0.3
  # The factual P64 state is the single motion owner. Appearance consumes that
  # state and must not introduce a second action skip.
  appearance_action_residual_scale: 0.0
  view_hidden: 768
  view_heads: 12
  view_ff_mult: 2.5
  max_action_groups: 8
  max_action_dim: 16
  max_state_dim: 32
  max_action_substeps: 128
  max_policy_queries: 256
  max_group_id: 128
  max_embodiments: 512
  max_action_semantic_id: 64
  max_state_semantic_id: 64
  time_fourier_dim: 128
  time_min_period_s: 0.01
  time_max_period_s: 120.0
  max_aux_tokens: 16
  aux_dim: 256
  max_aux_type_id: 128
  # Decode a compact set of training horizons. The full K8 factual state and
  # detail targets are still trained; serving can render any requested horizon.
  rgb_hidden: 512
  rgb_res_blocks: 1
  rgb_decode_chunk_size: 32
  rgb_size: 256
  rgb_decode_indices: [0, 2, 4, 7]
  # Canonical V7-style direct future-state rendering is the base. No context
  # copy, learned flow, disocclusion, blend, or renderer-only action owner.
  rgb_context_enabled: false
  rgb_context_alignment_enabled: false
  rgb_render_action_free_prior: false
  rgb_context_action_scale: 0.0
  rgb_context_appearance_delta_scale: 0.0
  # P256 is a bounded high-frequency correction. Zero detail exactly recovers
  # the direct factual-state decoder, so it cannot suppress motion.
  rgb_detail_residual_scale: 0.25
  geom_hidden: 512
  appearance_enabled: true
  appearance_P: 256
  appearance_context_frames: 4
  appearance_hidden: 256
  appearance_layers: 2
  appearance_heads: 8
  appearance_ff_mult: 2.0
  appearance_autoregressive_steps: 2
  appearance_flow_aligned_detail: false
  appearance_state_detail: true
  appearance_detail_dim: 256
  dropout: 0.0
  activation_checkpointing: true
